# Biblioteca_API

Para executar o programa você deve primeiro criar um ambiente virtual (venv) com os seguintes comandos: 
  python -m venv .venv
  .venv\Scripts\Activate.ps1

Em seguida instale o FastAPI:
  pip install "fastapi[standard]"

Agora execute o programa com:
  fastapi dev main.py
